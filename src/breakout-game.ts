import { html, LitElement } from "lit";
import { customElement } from "lit/decorators.js";

@customElement("breakout-game")
export class BreakoutGame extends LitElement {
  render() {
    return html`<canvas>hello</canvas>`;
  }
}
